import { FC } from "react";

import "./NotFound.scss";

const NotFound: FC = () => {
  return (
    <section className="not-found">
      <h3 className="not-found-text">Страница не найдена!</h3>
    </section>
  );
};

export default NotFound;
